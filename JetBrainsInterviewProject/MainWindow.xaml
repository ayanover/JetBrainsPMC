<Window x:Class="JetBrainsInterviewProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JetBrainsInterviewProject"
        mc:Ignorable="d"
        Title="Shell Command Executor" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0" Content="Enter Command:" FontWeight="Bold"/>
        
        <TextBox Grid.Row="1" Grid.Column="0" x:Name="CommandInputTextBox" 
                 FontFamily="Consolas" FontSize="14" Padding="5"
                 KeyDown="CommandInput_KeyDown"/>
        
        <Button Grid.Row="1" Grid.Column="1" x:Name="ExecuteButton" Content="Execute" 
                Padding="15,5" Margin="10,0,0,0" Click="ExecuteButton_Click"/>
        
        <Label Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
               Content="Command Output:" FontWeight="Bold" VerticalAlignment="Top"/>
        
        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,25,0,0" 
                BorderBrush="LightGray" BorderThickness="1">
            <RichTextBox x:Name="CommandOutput" IsReadOnly="True"
                       FontFamily="Consolas" FontSize="14" Background="#1E1E1E"
                       VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                       VerticalScrollBarVisibility="Auto" Padding="5">
                <RichTextBox.Resources>
                    <Style TargetType="Paragraph">
                        <Setter Property="Margin" Value="0"/>
                    </Style>
                </RichTextBox.Resources>
                <FlowDocument>
                    <Paragraph>
                        <Run Foreground="#CCCCCC" Text="Command output will appear here..."/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>
        </Border>
        
        <StatusBar Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Height="25">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>